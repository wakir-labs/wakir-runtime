#!/usr/bin/env python3
# SPDX-License-Identifier: BUSL-1.1
# Copyright (c) 2026 Callandor GmbH and contributors
#
# This file is part of a BUSL-1.1-licensed subtree (see wat/LICENSE-BSL.md).
# It re-imports ``wirelang.persona_engine.routing_diff`` (BUSL-1.1) and is
# itself licensed under BUSL-1.1 for consistency with the imported
# implementation.
#
# Sprint-AB-Routing-Diff-CLI-MINI — Operator-facing CLI entrypoint for
# the ADR-0064 §4 A/B-test diff. Thin shim over
# ``wirelang.persona_engine.routing_diff:main``.
#
# The hyphen in this filename makes a direct ``import`` syntactically
# impossible; this file does the path setup and delegates to the
# Wirelang-side module.
#
# See ``wirelang/persona_engine/routing_diff.py`` module docstring for the
# full contract (envelope shape, cost-weight model, consistency-score,
# PASS/FAIL verdict semantics).

import sys
from pathlib import Path

_HERE = Path(__file__).resolve().parent
_REPO_ROOT = _HERE.parent
if str(_REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(_REPO_ROOT))

from wirelang.persona_engine.routing_diff import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
