#!/usr/bin/env python3
# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 Callandor GmbH and contributors
#
# Sprint-Pengine-10 OI-PEFR-7 — Mira-Dispatcher Side-CLI entrypoint.
# Thin shim over ``wirelang.cli.mira_dispatch``.
#
# See ``wirelang/cli/mira_dispatch.py`` module docstring and
# ``wirelang/specs/bridge-forward-pipe-v1.md`` §4 for the full contract.

import sys
from pathlib import Path

_HERE = Path(__file__).resolve().parent
_REPO_ROOT = _HERE.parent
if str(_REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(_REPO_ROOT))

from wirelang.cli.mira_dispatch import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
